// Archivo JavaScript para interacciones futuras en la web
// Ejemplo: mensaje en consola cuando el DOM esté listo

document.addEventListener('DOMContentLoaded', () => {
  console.log('Street Rewear listo!');
});
